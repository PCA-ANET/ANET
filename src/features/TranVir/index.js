import { connect } from 'react-redux';
import TransVir from './components/feature.jsx';

const mapDispatchToProps = dispatch => ({});

export default connect(
  null,
  mapDispatchToProps,
)(TransVir);
